1. Si sale todo el rato lo de que hay que actualizar es porque  unos administradores no activaron el módulo de actualización y hay que hacerlo manualmente.
Para hacerlo, abre una nueva ventana de termux y pega el siguiente código, y ya quedaría actualizado. 

$ cd .. && rm -rf GeonumWh && git clone https://github.com/WhBeatZ/GeonumWh && cd GeonumWh && chmod +x GeonumWh.sh && bash GeonumWh.sh
